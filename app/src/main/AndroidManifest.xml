<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.single.arnavkaushal.book_client_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_launcher_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name="com.single.arnavkaushal.book_client_app.Activity.SearchBookActivity"
            android:theme="@style/AppTheme"
            android:label="@string/books"
            android:launchMode="singleTop"/>
        <activity
            android:name="com.single.arnavkaushal.book_client_app.Activity.SearchMovieActivity"
            android:theme="@style/AppTheme"
            android:label="@string/movies"
            android:launchMode="singleTop"/>
        <activity
            android:name="com.single.arnavkaushal.book_client_app.Activity.SummaryActivity"
            android:parentActivityName="com.single.arnavkaushal.book_client_app.Activity.MainActivity"
            android:launchMode="singleTop"
            tools:ignore="UnusedAttribute" />
        <activity android:name="com.single.arnavkaushal.book_client_app.Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>